module.exports = {
  i18n: {
    // These are all the locales to support in application
    locales: ['en', 'es'],
    // This is the default locale to be used when visiting
    // a non-locale prefixed path e.g. `/experience`
    defaultLocale: 'en',
  },
}